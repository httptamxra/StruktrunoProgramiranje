#include <stdio.h>
#include <stdlib.h>
//Napishi programa kade shto preku tastatura kje se vnesat b,h.
// Pronajdi ja nivanata plostina so formulata.
int main()
{
    int b,h;
    scanf("%d", &b);
    scanf("%d", &h);
    int P = b*h;
    printf("The area of A is: %d",P);
    return 0;
}
