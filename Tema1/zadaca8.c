#include <stdio.h>
#include <math.h>
//Napishi programa kade shto preku tastatura kje se vnesat a,b,h.
// Pronajdi ja nivanata plostina so formulata.
int main()
{
    int a,b,c;
    scanf("%d",&a);
    scanf ("%d", &b);
    scanf ("%d", &c);
    int P = (a+b)/2 *2;
    printf ("The area is: %d", P);
    return 0;
}
